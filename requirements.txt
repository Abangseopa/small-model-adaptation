# Small Model Adaptation — dependencies
#
# Exactly what the project code actually imports (verified by grep over
# the whole repo). Not installed automatically — see README "Setup".

# Stages 3-7: model/tokenizer loading, generation
torch
transformers

# Stages 4, 5, 7: LoRA adapter training and loading
peft

# Stage 8: automated test suite
pytest
